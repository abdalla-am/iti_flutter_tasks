import 'package:flutter/material.dart';
class Chat {
  String? name, text, url, time ,urlTextPhoto;
  Chat(
      {required this.name,
      required this.text,
      required this.time,
      required this.url, this.urlTextPhoto
      });
}
